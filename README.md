# Template for Single Page Applications on Google App Engine

Start frontend dev server:
- `npm start`

Start python dev-server
- `npm run backend`

Run js tests then python tests
- `npm test`

Build && deploys to Google Appengine
- `npm run deploy`
